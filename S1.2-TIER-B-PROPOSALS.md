---
stage: S1.2
operator_action_required: true
---

# S1.2 — Tier B proposals (operator-confirm)

Per `feedback_destructive_instruction_preserve_intent` and plan 01
§"Cleanup playbook" Tier B: the 6 frontmatter category violations in
`brain/projects/trafficGame/themes/` are **flagged** by `brain-lint`,
**never** auto-rewritten in this autonomous pass.

After waking, the operator reviews each proposal below and applies the
remapping (or chooses a different remap, or declines). Suggested
sequence:

```bash
cd _worktrees/s1.2-brain-hygiene
# Inspect each flagged theme.
forge brain lint --scope project-only --project trafficGame
# Apply (manually — sed for each).
```

The current category whitelist is `pattern | antipattern | decision |
operation | reference`. The categories `snapshot`, `process`, and
`bug-candidate` exist in trafficGame but are not whitelisted (per
plan 01 §"Problem" — silent rule break).

## Proposed remappings

### Remap 1 — `snapshot` → `reference`

Architectural / system-state captures are closer to "reference" docs
("system overviews, profiles, comparisons, glossaries" per INDEX.md)
than to "operations" (which are about *running* the system, not
*describing* it). Four files:

| File | Title | Current | Proposed |
|---|---|---|---|
| `brain/projects/trafficGame/themes/2026-05-10-campaign-mode-state.md` | campaign is a connected CampaignGraph (5-map directed convergent-AND world) | `snapshot` | `reference` |
| `brain/projects/trafficGame/themes/2026-05-10-mvp-architecture-snapshot.md` | MVP architecture snapshot (May 2026, post-extraction) | `snapshot` | `reference` |
| `brain/projects/trafficGame/themes/2026-05-10-recent-extractions-and-tech-debt.md` | recent extractions reduced Game.ts and what tech debt remains | `snapshot` | `reference` |
| `brain/projects/trafficGame/themes/2026-05-10-traffic-location-data-model.md` | TrafficLocation has paired entry/exit ConnectionPoints with edge orientation | `snapshot` | `reference` |

### Remap 2 — `process` → `operation`

`operation` per plan 01 covers "how to run / monitor / recover the
system" — testing process fits this; it's "how we test trafficGame in
forge cycles". One file:

| File | Title | Current | Proposed |
|---|---|---|---|
| `brain/projects/trafficGame/themes/2026-05-10-test-stack-and-gates.md` | test stack is Vitest + Playwright with a strict TDD + visual-test discipline | `process` | `operation` |

### Remap 3 — `bug-candidate` → `antipattern`

A documented bug pitfall (`fillRect` overlay alpha-stacking) reads as
an antipattern — "proven approach that doesn't work" — more than a
reference. The body of this theme is exactly the kind of negative
guidance the antipattern category is for. One file:

| File | Title | Current | Proposed |
|---|---|---|---|
| `brain/projects/trafficGame/themes/2026-05-10-ui-canvas-overlay-pattern.md` | UI overlays paint translucent black via fillRect without clearRect first | `bug-candidate` | `antipattern` |

Note: the filename suffix `-pattern.md` is misleading given the
proposed remap to `antipattern`. Renaming the file is **not part of
this pass** (filename rename ripples through `category-index` entries
+ any wikilinks). Operator may rename in a follow-up.

## How to apply

After review, the operator can apply remap-1 with:

```bash
cd _worktrees/s1.2-brain-hygiene
for f in \
  brain/projects/trafficGame/themes/2026-05-10-campaign-mode-state.md \
  brain/projects/trafficGame/themes/2026-05-10-mvp-architecture-snapshot.md \
  brain/projects/trafficGame/themes/2026-05-10-recent-extractions-and-tech-debt.md \
  brain/projects/trafficGame/themes/2026-05-10-traffic-location-data-model.md; do
  sed -i 's/^category: snapshot$/category: reference/' "$f"
done

sed -i 's/^category: process$/category: operation/' \
  brain/projects/trafficGame/themes/2026-05-10-test-stack-and-gates.md

sed -i 's/^category: bug-candidate$/category: antipattern/' \
  brain/projects/trafficGame/themes/2026-05-10-ui-canvas-overlay-pattern.md

# Re-lint to confirm errors cleared.
node --experimental-strip-types orchestrator/brain-lint.ts --scope project-only --project trafficGame
```

After this, the 6 frontmatter errors should be gone. The 8
`checkIndexSync` flags will also need the operator to add each
remapped theme to the appropriate `brain/projects/trafficGame/<category>.md`
index file (or rely on a future `--fix` mode that does it
automatically — currently a stub).

## What this pass intentionally does NOT do

- **No auto-rewriting of category values.** Per the operator's standing
  destructive-instruction rule. The proposed mappings could be wrong —
  the operator may decide `snapshot` is itself worth whitelisting
  (Open Q1 in plan 01). Leaving the decision in the operator's hands.
- **No filename renames.** Touching filenames is a multi-file ripple.
- **No `LINT.md` whitelist expansion.** That decision needs an ADR or a
  whitelist amendment commit, distinct from cleanup hygiene.
