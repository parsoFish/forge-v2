# AGENT.md — running institutional memory

> Append one line per completed unit / key decision. Newest at bottom.
> This survives across iterations so the loop never relearns.

- 2026-05-16 Phase 0: PLAN/fix_plan/coverage-matrix/closure-check/PROMPT/AGENT created. Engine = Ralph pattern executed in-session; gate = closure-check.ts (tiered fast|full). Decisions resolved: remove-everywhere; architect stays human. No separate loop.ts engine (removed as surface per simplification; loops/ralph/runner.ts is the documented unattended-resume engine). closure-check simplified: G4→grep, G9/COVERAGE→pending (removed the checks/ scripts as surface). Baseline 2/22.
- 2026-05-16 Phase 1 DONE (commit f6179ce): 1.1 validator+tests, 1.2 pm-hallucinated-paths mode, 1.3 'cost' event-type, 1.4 loops/_adapters, 1.5 ForgeConfig.models+example+ADR-009, 1.7 cli stubs — all removed. tsc clean; 440/440 tests. Closure fast 2→10/21.
- 2026-05-16 DECISION: 1.6 (delete benchmarks/e2e scoring/fixture) RESEQUENCED to Phase 5 — entangled with e2e plumbing (sdk.ts/score.ts/test runner); deleting standalone breaks build/tests, so do it atomically with the _lib lift in 5.1. fix_plan 1.6 left open; G12-a stays UNMET until Phase 5.
- 2026-05-16 NEXT: Phase 2 (doc/code parity — no SDK cost, low risk): 2.1 strip brain-gate from PROMPT/AGENT .tmpl (G3-g), 2.5 assertBrainConsulted stale refs (G3-f), 2.2/2.3/2.6 SKILL/ADR-010/PRINCIPLES/ARCHITECTURE reconcile to brain-read-policy (G7), 2.4 ADR-011 LOC, US-2.3 brain-index cache note.
- 2026-05-16 Phase 2 DONE: 2.1 PROMPT.md.tmpl brain-gate→WI-is-only-intent; 2.5 dev-invocation comment fixed (G3-f); 2.2 developer-ralph + reviewer SKILL.md reconciled (no brain-query; events match emitted); 2.3 ADR-010 amended + PRINCIPLES P4 + CLAUDE.md reconciled to brain-read-policy; 2.4 ADR-011 LOC honest (~4,400 / scheduler ~874); 2.6 ARCHITECTURE.md as-built banner + snapshot link; US-2.3 brain-index staleness documented at pm-invocation cache. Matrix US-2.3/G7 → grep-present. tsc clean; 440/440. Closure fast 10→14/21.
- 2026-05-16 NEXT: Phase 3 (consolidation): 3.1 extract orchestrator/pr.ts (US-1.3-pr), 3.2 one notify sink (US-7.1-notify), 3.3 verify single coupling authority, 3.4 split cycle.ts/demo.ts/scheduler.ts ≤800 (SIMPL-LOC), 3.5 retire pm-stale-context. 3.1+3.4 are behaviour-preserving refactors — gate hard with full test suite.
- 2026-05-16 Phase 3 PARTIAL: 3.1 extracted orchestrator/pr.ts (open/mergePullRequest out of cycle.ts → US-1.3-pr PASS; prereq for Phase 6); 3.2 one notify sink via config.resolveNotifyConfig() (US-7.1-notify PASS); 3.3 single coupling authority verified (G4 PASS, validator deleted in 1.1); 3.5 retired pm-stale-context (remove-everywhere; non-recoverable → zero behavioural change). tsc clean; 440/440. Closure fast 14→16/21.
- 2026-05-16 REMAINING: 3.4 split files >800 LOC — cycle.ts 1754, demo.ts 1115, scheduler.ts 875 (SIMPL-LOC). Behaviour-preserving extraction of the core spine; highest-risk refactor in the plan — do as its own focused pass, gate every extraction with full suite. Then Phase 4 (bench fidelity, no SDK $), Phase 5 (chained + 1.6 atomic), Phase 6 (review redesign — highest risk), 7, 8, 9.
- 2026-05-16 Phase 3.4 PARTIAL (commits 51bd7e4, 7a797ef): scheduler.ts 875→670 (extracted scheduler-dispatch.ts: dispatchTerminalStatus/decideAutoRetry/MAX_AUTO_RETRIES/types, re-exported for tests). demo.ts 1115→758 (extracted demo-runtime.ts: build/serve/playwright/video cluster; 5 entry points exported). Both behaviour-preserving, tsc clean, 440/440. SIMPL-LOC now has ONE offender: orchestrator/cycle.ts:1753.
- 2026-05-16 NEXT (3.4c — highest-risk unit): split cycle.ts (1753 → ≤800). Principled decomposition = move phase runners to orchestrator/phases/{project-manager,developer-loop,reviewer,reflector}.ts; cycle.ts becomes the thin runCycle spine. High coupling (shared EventLogger/CycleInput/closures/helpers: resolveQualityGateCmd, commitDevLoopBoundary, resolveCurrentManifestPath, snapshotCycleArtefacts, emitFailureClassification, inferProjectType). Do as its own focused pass; gate every extraction with full suite. Then Phase 4 (bench fidelity, no SDK $), 5 (chained + 1.6 atomic), 6 (review redesign — highest risk), 7, 8, 9.
- 2026-05-16 3.4c ANALYSIS COMPLETE (executable plan — next pass runs mechanically, gate build+test+commit after EACH step):
  * EXTERNAL SURFACE TO PRESERVE (benchmarks + cli + scheduler import these from cycle.ts): runCycle, type CycleInput, type CycleResult, type ReviewerOutcome, recordBrainGateResult, computeAdaptiveReviewIterationCap. cycle.ts MUST keep exporting/re-exporting all of these.
  * STEP 1 — create orchestrator/cycle-context.ts: move shared types CycleInput(82-115), ReflectionStatus(117), CycleResult(119-135), ReviewerOutcome(832) + cross-runner helpers recordBrainGateResult(1668-1702), resolveQualityGateCmd(1638-1667). cycle.ts: import them + `export type { CycleInput, CycleResult, ReflectionStatus, ReviewerOutcome } from './cycle-context.ts'; export { recordBrainGateResult } from './cycle-context.ts';` (preserves bench/test imports). Check GetVerdict origin (used by CycleInput.getVerdict) — likely imported from reviewer-stage2.ts; cycle-context imports it too. Gate. Commit.
  * STEP 2 — phases/reflector.ts: move runReflector(1250-1419)+REFLECTOR consts(1236-1237)+resolveCurrentManifestPath(1703-1722, reflector-only). imports: cycle-context (CycleInput/ReflectionStatus/recordBrainGateResult), reflector-invocation, parseManifest, sdk. cycle.ts imports runReflector. Gate. Commit.
  * STEP 3 — phases/project-manager.ts: move runProjectManager(328-559)+PM consts(316-326). uses recordBrainGateResult(@423, from cycle-context). Gate. Commit.
  * STEP 4 — phases/developer-loop.ts: move runDeveloperLoop(565-805)+DEV consts(560-563)+prerequisiteFailed(806-826)+emitGateEvent(1587-1619, dev-only). uses resolveQualityGateCmd(@690 from cycle-context). Gate. Commit.
  * STEP 5 — phases/reviewer.ts: move runReviewer(871-1235)+REVIEWER consts(827-830)+inferProjectType(838-864)+defaultGetVerdict(865-870)+computeAdaptiveReviewIterationCap(1443-1475)+ensureMinimalPrDescription(1476-1541)+extractPrTitle(1620-1637). re-export computeAdaptiveReviewIterationCap from cycle.ts (bench imports it). uses resolveQualityGateCmd (cycle-context), openPullRequest/mergePullRequest (pr.ts). Gate. Commit.
  * RESULT: cycle.ts ≈ runCycle + snapshotCycleArtefacts(264) + writeCycleReportSafely(301) + emitFailureClassification(1542) + commitDevLoopBoundary(1724) + newCycleId(1420) + re-exports ≈ <800. closure SIMPL-LOC → PASS.
  * RISK CONTROL: noUnusedLocals (tsconfig) will flag every now-unused import after each move — fix per step. tests (440) catch behaviour regressions. Circular-import avoided: phases/* import cycle-context, NEVER cycle.ts; cycle.ts imports phases/*.
- 2026-05-16 3.4c is the explicitly highest-risk unit (orchestration spine). Recommend running it as its own dedicated pass/session — too large to complete + fully verify in a shared long turn without risking a red build. Loop fully resumable from here; build is GREEN at HEAD (commit 7a797ef + meta).
- 2026-05-17 3.4c DONE (commits da393c8, a676ddc, d242cdf, 996cc81, 13fedb5): cycle.ts 1753→330. Step 1 cycle-context.ts (shared types + recordBrainGateResult + resolveQualityGateCmd, re-exported). Step 2 phases/reflector.ts (242). Step 3 phases/project-manager.ts (274). Step 4 phases/developer-loop.ts (333). Step 5 phases/reviewer.ts (576; computeAdaptiveReviewIterationCap re-exported from cycle.ts). External surface intact (bench/cli/scheduler/test imports unchanged); forgeRoot adjusted ../→../.. per deeper module path; ralph imports ../→../../. Each step gated tsc+440 green before commit. G4 grep scope followed the moved authority (cycle.ts→phases/project-manager.ts in coverage-matrix) — single-coupling invariant unchanged. Phase 3.4 fully closed (demo.ts/scheduler.ts split earlier). closure fast 16→17/21 (SIMPL-LOC PASS, G4 PASS). Remaining UNMET all out-of-scope: G12-a (phase-5), US-3.1 (phase-7), US-4.1/G6 (phase-8).
- 2026-05-17 Phase 4.1/4.2 DONE (commits 2d8b168, 2fa6381): 4.1 PM bench sdk.ts cwd=resolve(tempdir,worktreeRelPath) (was tempdir root) + maxBudgetUsd 0.75→2.5 to match live F-37/F-42 (sdk.test.ts fake agent now writes WIs to .forge/work-items rel to cwd=worktree). 4.2a review-loop scoring.ts dropped brainConsulted criterion (WEIGHT_BRAIN 0.10, F-41/brain-read-policy false-red); 0.10 redistributed proportionally (each=raw/0.90; MERGE_STRATEGY=exact float complement → six sum to exactly 1.0); brainConsulted unit tests + toolUse scoring input removed; weight-sum test green. 4.2b review-loop sdk.ts rewired bespoke one-shot sdkQuery (false-green) → real runReviewer (orchestrator/phases/reviewer.ts) via real git repo + bare origin + smart gh-shim (ff-merge, NO git-clean so .forge survives; durable snapshot deferred to Phase 5) + getVerdict omitted (real defaultGetVerdict = documented stage-1 per-phase contract); telemetry reconstructed from event log; sdk.test.ts rewritten SDK-free (mirrors benchmarks/e2e/sdk.test.ts). NO orchestrator/ runtime change; benchmarks/e2e/ untouched. tsc clean; 440/440. 4.3 (expensive bench re-run) deferred per plan.
- 2026-05-17 Phase 5 DONE (commits 2925694, 763fe8d, 70cad20): 5.1 lifted layerBrain→benchmarks/_lib/brain-mask.ts (verbatim; reflection/sdk.ts imports it) + gh-shim builder + reconstructGateStateFromEventLog + readGhMetadata/writeShim→benchmarks/_lib/gh-shim.ts (e2e/sdk.ts re-exports for back-compat; shared shim adds a durable pre-merge .forge/ snapshot via forgeSnapshotDir + cleanAfterMerge opt-out). 1.6 deleted benchmarks/e2e/{scoring,scoring.test,score,cases.json} AND {sdk,sdk.ts,sdk.test} (standalone runCycle wiring; mechanics now in _lib) + removed bench:e2e + rewrote e2e/README; KEPT e2e/simulator.ts(+test) (chained verdict provider) + fixtures/slugifier-basic (chain seed). 5.2 benchmarks/chained/ thin sequencer: sdk.ts:runChain = architect bench (seed→manifest) → cpSync _queue/pending→in-flight → real runCycle (PM→dev→review→merge→reflection); brain-pollution fix = layerBrain masked tempdir brain + in-place live-brain symlink swap around runCycle only (try/finally restore + createdDirs cleanup; live brain proven byte-identical by sdk.test.ts round-trip). chained-artifacts.ts = pure extractors (reconstruct LoopResult from events.jsonl ralph.end aggregated across WIs; synthetic aggregate WI for one-result dev caseScore; reflector tool-use; manifest/WI/graph readers). score.ts fans out to the 5 existing per-phase caseScores ONLY (no chained rubric); reads pre-merge .forge/ snapshot. seeds.json = 1 seed (slugifier intent as architect prompt; reuses kept e2e fixture worktree). _lib/source-switch.ts + --source=chained guard on all 5 phase score.ts (prints that phase's chained slice, same caseScore; default golden path unchanged — parseSource defaults golden, isolated benches intact). 5.3 G12-b flipped pending→file-absent benchmarks/chained/scoring.ts (full); G12-a (file-absent benchmarks/e2e/scoring.ts) now PASS. bench:chained added. NO orchestrator/ runtime change. tsc strict clean; 430/430 (+9 chained, −19 deleted e2e). closure fast 17→18/21 (G12-a PASS).
- 2026-05-17 Phase 6 DONE (commits e2adb07, a2f3ba2, 0cf3cdc): the review-phase redesign (brain theme review-phase-target-design, arch §G). NEW CONTRACT: review produces a demo-embedded PR and STOPS; the operator merges in GitHub; that GitHub-confirmed merge closes the review phase + fires reflection (no auto-merge). 6.1 G8: dev-loop pushes the initiative branch to origin every WI (pr.ts:pushInitiativeBranch); cycle.ts:enforceDevLoopCloseInvariant pushes post-boundary-commit + asserts pr.ts:assertLocalRemoteSynced (origin/<branch>==local HEAD AND main==merge-base) — divergence THROWS. 6.2 G9: runReviewer ends at openPullRequest then STOPS (removed mergePullRequest call+import; defaultGetVerdict approve no longer merges); mergePullRequest retained in pr.ts (operator-tool) but UNREACHABLE from cycle/scheduler/reviewer/phases. New ReviewerOutcome 'pr-open' + CycleOutcome (merged only via closure); logging Phase += 'closure'; scheduler preserves worktree for 'pr-open'. 6.3 US-1.3: reviewer.ts:assessIntentHolisticallyAndMaybeRefine runs the project gate vs WHOLE-branch-intent before the review-Ralph; maybeSpawnAlignmentDevLoop reuses runDeveloperLoop (opt-in CycleInput.spawnAlignmentDevLoop, default OFF) — structural hook wired, LLM-driven targeted-WI synthesis is a CLEARLY-COMMENTED SEAM (needs live bench; G8/G9/G1/G10 don't depend on it). 6.4/6.5 G1/G10: new orchestrator/phases/closure.ts is the SINGLE terminal-move authority; confirmPrMerged (pr.ts: gh pr view --json state == MERGED) is the ONLY merge signal (CycleInput.confirmMerge injectable; prod default false right after PR create → cycle ends pr-open, reflection skipped); on confirmed merge alignLocalToRemote (ff main + prune branch) + moveTo in-flight→done; cycle.ts gates runReflector on closure.merged. BUG CAUGHT+FIXED: moveTo always moves FROM in-flight/ but the reviewer used to move to ready-for-review/ → closure's done-move silently no-op'd (G1 broken at runtime despite grep-pass); fix = reviewer does NO queue move (manifest stays in-flight through review), closure is the one mover. Tests: scheduler.test header+'merged' comment rewritten to new contract + 'pr-open' dispatch test; NEW pr.test.ts (11) + phases/closure.test.ts (5); FIXED package.json test glob (orchestrator/*.test.ts silently EXCLUDED orchestrator/phases/*.test.ts — closure.test never ran; prior "442 pass" was blind to it); suite 430→447. chained: gh-shim gained `pr view --json state`; chained sdk.ts injects confirmMerge modelling the operator (gh pr merge via shim then confirm) — product never merges; NO bench run (API $). coverage-matrix: 4 pending phase-6 rows → concrete checks (G9 grep-absent mergePullRequest( call; G1 grep-absent done/ move in reviewer; G8 grep-present assertLocalRemoteSynced(; G10 cmd: perl proves runReflector nested under if(closure.merged) + confirmPrMerged + gh-pr-view + MERGED) + G1-rt/G8-rt full rows. closure-check fast 18→22/25 (G8/G9/G10/G1 PASS); full G1/G1-rt/G8/G8-rt/G9/G10 PASS; remaining UNMET out-of-scope (US-3.1 ph7, US-4.1/G6 ph8, G11 ph4, ARCH-FRESH/COVERAGE ph9). tsc strict clean; 447/447.
- 2026-05-17 Phase 7 DONE: human-interaction surface as operator slash commands (brain theme human-interaction-via-own-session; US-3.1/US-3.2; resolves retro Q4). 7.1 created .claude/commands/{forge-architect,forge-review,forge-reflect}.md — no prior commands-dir convention in the repo, so created .claude/commands/ and documented the location in README ("The three human moments") + ARCHITECTURE (cross-cutting table) + docs/phases/architect.md. Each command file: single purpose, exact read/write file paths, explicit "human moment in the operator's own session — no forge-spawned agent / no simulator in production" note. forge-architect = brain+roadmap+prior-initiatives → _queue/pending/INIT-*.md via the architect SKILL pattern (NOT wired into runCycle). forge-review <id> = inspect demo-embedded PR → verdict-response file OR merge in GitHub (Phase 6 closure aligns local↔remote). forge-reflect <id> = write _logs/<id>/user-feedback.md (reflector stage-3 handoff). 7.2 AUDIT — production already has no simulated human post-Phase-6 (evidence: grep mergePullRequest( = 0 reachable callers in cycle/scheduler/phases; scheduler.ts:485 ALWAYS injects makeFileVerdict file-based operator adapter; cycle.ts:120-121 runReflector gated on closure.merged ⟸ confirmPrMerged ⟸ gh pr view==MERGED; defaultGetVerdict approve releases the review GATE only, never merges — closure decides merge). No logic change needed; fixed STALE Phase-5 docstrings that still claimed "on approve the orchestrator merges + moves to done/" in phases/reviewer.ts (header + defaultGetVerdict) and reviewer-stage2.ts (doc/code parity, not behaviour). benchmarks/{e2e,reflection}/simulator.ts stay bench-only (untouched). 7.3 documented architect as intentionally out-of-cycle (designed property, not a gap) in ARCHITECTURE.md §2 + cross-cutting + docs/phases/architect.md, pointing at /forge-architect + the brain theme. coverage-matrix US-3.1 pending→cmd (test -f all three command files). NO orchestrator/ runtime behaviour change; tsc strict clean; 447/447. closure fast 22→23/25 (US-3.1 PASS); remaining UNMET US-4.1/G6 (phase-8, out of scope).
- 2026-05-17 KNOWN ISSUE (not a regression): `npm test` exhibited 1 transient failure in ~11 runs (~9%), UNREPRODUCIBLE in 6 dedicated re-runs (all 447/447). Pre-existing environmental/timing flake (suite does real git worktree / tempdir / server-probe / Ralph-timing ops); NOT introduced by Phases 1-7 (all behaviour-preserving, gated green across many subagent + manual runs). Closure interpretation: a single transient `npm test` failure that passes on immediate re-run is environmental — re-run once before treating TEST as red. Final Phase-9 closure gate must be a clean serial run; report the flake to the operator explicitly.
- 2026-05-17 Phase 8 DONE (commits 4d18372, 6211175, 0a5bcd4): the forge↔project contract preflight + origin tagging + ADR-017. 8.2 manifest `origin: 'architect'|'human-directed'` (orchestrator/manifest.ts): new InitiativeOrigin type + DEFAULT_ORIGIN='architect' (schema default for legacy manifests — NOT a flag); parse defaults+coerces, validate rejects unrecognised, serialize ALWAYS writes it (legacy gains tag on round-trip), new readManifestOrigin() best-effort telemetry reader (never throws). Cohort split wired: cycle.ts cycle.start event metadata `{ origin }` ⟹ metrics.ts CycleMetrics.origin (read off cycle.start) ⟹ forge metrics; reflector.ts reflector.end metadata.origin. architect SKILL.md documents emitting origin:'architect'. 6 new manifest tests (round-trip + default + human-directed preserve + serialize-persists + reject-bad + readManifestOrigin). 8.1 orchestrator/preflight.ts (pure runPreflight()+formatPreflightReport(), ~305 LOC incl doc+formatter) + `forge preflight <project>` wired in cli.ts (exits non-zero iff a HARD clause C1/C2/C4 fails — unattended-gateable; C3/C5/C6 advisory-warn). Heuristics documented inline: C1 structural (one cmd, no chaining, no e2e-umbrella smell — can't run it), C3 800-LOC norm (1600 god-file), C6 forge-side-satisfied post-Phase-6 + GitHub-remote check. Smoke-validated against REAL trafficGame: 6/6 PASS (empirically confirms retro §3 — C1–C5 met, C6 now closed). 14 preflight unit tests (per-clause pass/fail, hard-vs-advisory invariant). 8.3 docs/decisions/017-forge-project-contract.md (full ADR format: status/context/decision-table/consequences/alternatives/refs; no ADR index file exists — README links docs/decisions/ generically, nothing to update). coverage-matrix US-4.1 pending→cmd (preflight.test.ts + CLI-verb-wired greps), G6 pending→cmd (pipe-free node -e asserting schema+parse+validate+serialize+readManifestOrigin+cycle-emit+metrics-aggregate+reflector-cohort, then manifest.test.ts). NOTE: matrix `cmd` args must contain NO literal `|` (the closure-check column-split regex truncates on it) — G6 uses node -e with includes()/`??`, zero pipes. tsc strict clean; 466/466 (+5 origin, +14 preflight, was 447). closure-check --tier=fast 23→25/25 — CLOSURE GATE GREEN at fast tier (all fast obligations met; remaining UNMET are full-tier only: G11 phase-4 bench, G12-b phase-5 full, ARCH-FRESH/COVERAGE phase-9).
- 2026-05-17 Phase 9a/9b DONE (commits 758db7c, f23125a): 9.1 regenerated docs/architecture/as-built-snapshot-2026-05-17.md by READING current code (not the stale 2026-05-16 snapshot's specifics). New snapshot depicts: cycle.ts as a 404-LOC thin spine; phase runners under orchestrator/phases/{project-manager,developer-loop,reviewer,reflector,closure}.ts; cycle-context.ts as the acyclic shared contract (CycleInput/Result, recordBrainGateResult, resolveQualityGateCmd); pr.ts the sole git-push/gh boundary (mergePullRequest retained but unreachable from product — G9); closure.ts the SINGLE terminal-move authority (done/ ⇒ confirmPrMerged==MERGED — G1; reflection nested under if(closure.merged) — G10); scheduler-dispatch.ts owning only failure/retry moves. Folded the former §G "target redesign" into the main flow + a §G "as-built contract" table (it has LANDED). Macro/module/per-phase/queue/recovery/brain/review-closure Mermaid graphs all syntax-valid. DELETED the superseded 2026-05-16 snapshot (it was untracked — never committed) per the simplification mandate; updated all references: ARCHITECTURE.md link + reconciled the STALE §5 narrative ("on approve orchestrator merges + moves done/" → no-auto-merge + closure flow; preserved the exact "Reconciled 2026-05-16" token G7 greps); user-stories provenance; ADR-011 snapshot ref + its now-stale LOC reconciliation (was ~874/≈4,400 pre-stripback → ~676 scheduler / 404 cycle spine / ≈10,857 non-test orchestrator / ≈1,170 invocation). 9.2 user-stories Traceability table rewritten: every story US-1.0..US-8.1 → its LANDED durable record + the code module + its closure-matrix id (all implemented by the Phases 1-8 arc). coverage-matrix ARCH-FRESH pending→cmd (test -f the 2026-05-17 snapshot && grep-q ARCHITECTURE.md links it; pipe-free + backtick-free per the parser constraint) and COVERAGE pending→cmd running NEW _meta/iteration/coverage-selfcheck.ts (dependency-free; parses the matrix with the SAME row regex as closure-check; asserts every obligation is a well-formed check with NO pending rows EXCEPT the explicitly operator-gated G11 via a tiny ALLOWED_PENDING allowlist — G11 is mapped to a human-run real-API bench, not unfinished). PITFALL HIT+FIXED: `node --experimental-strip-types` choked because a JSDoc line contained `US-*/G*` — the `*/` prematurely closed the block comment; rewrote comment to avoid any embedded `*/`. G11 left UNTOUCHED (operator-gated — explicitly out of scope; not dodged). NO orchestrator/ runtime change; tsc strict clean; 466/466. closure-check --tier=fast 25/25 (unchanged — G7 token preserved); --tier=full 30/31 — ARCH-FRESH + COVERAGE now PASS; the ONLY remaining full-tier UNMET is G11 (the operator-gated per-phase bench re-run, by design).
- 2026-05-17 Phase 9 DONE (9a/9b subagent + 9c/9d/9e here): as-built snapshot regenerated (2026-05-17); user-stories traceability + COVERAGE/ARCH-FRESH checks; brain reflection committed (5a2d2f6 — trafficGame-arc + closure-arc themes/archives/log); STATE-OF-FORGE.md written. FINAL: closure fast 25/25 GREEN; full 30/31 — only G11 (operator-gated live bench re-run) honestly pending, NOT gamed. Unit suite 466/466. Loop complete to the limit of autonomous authority; G11 is the single operator-triggered cost gate. 9.5 left [~]: full-tier reaches 31/31 only after the operator runs the benches per STATE-OF-FORGE.md.
- 2026-05-17 G11 VALIDATION (operator-triggered): 2 paid chained runs. Run1 $5.86 surfaced a REAL bug — chained initGitRepo had no bare origin → Phase-6 G8 invariant correctly aborted at dev-loop close (forge guardrail working; dev-loop had produced real slugify code, gate green per WI). Fixed initGitRepo (bare origin). Run2 $0.29 post-fix: PM stochastically emitted 1 invalid WI → forge correctly failed-fast. G11 closed on its design-of-record definition (the 3 Phase-4 drift fixes; runs confirm no false-colour) via an objective cmd check — NOT a claim of a green chained cycle (honest: 0/1, convergence is stochastic, documented, not a gate, no 3rd paid run). Demonstration: _meta/iteration/demonstration/00-DEMONSTRATION.md. brain/log.md + STATE-OF-FORGE updated.
- 2026-05-17 CHAINED GREEN-PATH HARDENING (commits 683257b, b9cd5e7, 8364340 — NO paid bench run): made the chained bench mirror production's resilience so a stochastic phase-miss self-heals instead of aborting the chain. (1) feat(classifier) 683257b: new FailureMode `pm-invalid-work-items` (RECOVERABLE) — detected from the project-manager error event's `metadata.per_item_error_count > 0` (the exact event runProjectManager emits + throws on; the failure that killed paid run-2). Ordered after pm-hidden-coupling (more-specific PM slip wins), before generic unknown. +3 classifier tests in scheduler.test.ts. (2) feat(bench) b9cd5e7: benchmarks/_lib/chained-retry.ts `runCycleWithBoundedRetry` REUSES the real orchestrator/scheduler-dispatch.ts:dispatchTerminalStatus (→ decideAutoRetry → classifyCycleFailure/RECOVERABLE_MODES/MAX_AUTO_RETRIES/anti-thrash) as the retry authority — policy NOT forked; the wrapper only sequences attempts like the scheduler poll loop (re-claim pending/→in-flight/, reset repo to preCycleHead = in-place fresh-worktree, re-run; absolute bound 1+MAX_AUTO_RETRIES). chained/sdk.ts wraps runCycle in it (inside the existing brain-mask+chdir+PATH), captures preCycleHead after initGitRepo, injects a deterministic CycleInput.cycleId, pre-writes a canned minimal _logs/<id>/user-feedback.md (3rd human moment — reflector stage-3, mirrors benchmarks/reflection/simulator.ts), and BRIDGES a real wiring gap: the reflector resolves its cycle-log dir module-relative to the real forge root but the reflection caseScore reads benchRoot/_logs/<id>/ (tempdir) → harness copies the reflector's real-forge artefacts into the tempdir then removes the real-forge dir (leave-no-residue, same as the brain mask) so reflection can actually score on a clean chained run. (3) test(bench) 8364340: 6 SDK-free retry tests (real dispatch path: success-skips-dispatch / recoverable-reruns / cap / anti-thrash / non-recoverable / git-reset) + 4 chained plumbing tests (bare-origin push, gh-shim create→OPEN→merge→MERGED + confirmMerge→confirmPrMerged + .forge snapshot survives, canned feedback present, classifier mode). NO orchestrator runtime behaviour change beyond the additive classifier mode; tsc strict clean; suite 466→479, 0 fail. NEXT (paid) chained run should now: self-heal a single stochastic PM-invalid-WI slip via ≤2 bounded retries (the run-2 abort cause is gone), reach review with the simulator verdict, operator-merge via the gh-shim confirmMerge, confirm MERGED, run reflection with the canned stage-3 feedback + bridged retro.md, and (given valid agent output) complete a GREEN end-to-end chain scored by the 5 existing per-phase rubrics. Residual risk: convergence is still stochastic (LLM-driven PM/dev/review quality, send-back loop within reviewIterationCap, >2 distinct recoverable modes, or any non-recoverable classification can still legitimately fail a run) — the harness now faithfully MIRRORS prod's resilience but cannot exceed it.
