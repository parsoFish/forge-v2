# AGENT.md — running institutional memory

> Append one line per completed unit / key decision. Newest at bottom.
> This survives across iterations so the loop never relearns.

- 2026-05-16 Phase 0: PLAN/fix_plan/coverage-matrix/closure-check/PROMPT/AGENT created. Engine = Ralph pattern executed in-session; gate = closure-check.ts (tiered fast|full). Decisions resolved: remove-everywhere; architect stays human. No separate loop.ts engine (removed as surface per simplification; loops/ralph/runner.ts is the documented unattended-resume engine).
