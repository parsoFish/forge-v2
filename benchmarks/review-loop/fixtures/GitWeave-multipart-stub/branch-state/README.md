# GitWeave

GitHub-webhook aggregator. Ships a `splitOnBoundary` helper for multipart payloads.

## Quality gate

```
node --test --experimental-strip-types tests/multipart.test.ts
```

## Recent work

- WI-1 (complete): added `splitOnBoundary`. See `.forge/work-items/WI-1.md`.
